# Override: build locally instead of pulling from GHCR
# Usage: docker compose -f docker-compose.yml -f docker-compose.build.yml up --build
services:
  eyeris:
    build:
      context: .
      dockerfile: Dockerfile
    image: eyeris:dev
    container_name: eyeris-dev
